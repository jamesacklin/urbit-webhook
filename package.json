{
  "name": "urbit-webhook",
  "version": "0.0.1",
  "description": "A simple webhook server for Urbit. It listens for POST requests on /webhook, then sends a poke to a Groups channel.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "James Acklin <james.acklin@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
